// Copia as propriedades de o para p, e retorna p
function copy(o, p) {
    p = p || {}; // se nenhum o objeto é passado para p, usa um novo obejto criado.
    // Corpo da função vai aqui...
}

// function copy(o, p = {}) {...} EC6