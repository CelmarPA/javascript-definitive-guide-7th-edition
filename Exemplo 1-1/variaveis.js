let message = "hello";
let i = 0, j = 0, k = 0;
let x = 2, y = x * x; // Inicializadores podem usar variáveis declaradas previamente.

const H0 = 74; // Constante de Hubble (km/s/Mpc)
const c = 299792.458; // Velocidade da luz no vácuo (km/s)
const AU = 1.496E8; // Unidade Astronômica: distância até o sol (km)

