let matrix = [[1,2,3], [4,5,6], [7,8,9]];
console.log(matrix);

let sparseArray = [1,,,,5];
console.log(sparseArray.length)

let p = { x: 2.3, y: -1.2 }; // Um objeto com duas propriedades
let q = {}; // Um objeto vazio sem propriedades
q.x = 2.3; q.y = -1.2; // Agora q tem as mesmas propriedades que p

let rectangle = {
    upperLeft: { x: 2, y: 2 },
    lowerRight: { x: 4, y: 5}
};