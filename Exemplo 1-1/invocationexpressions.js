function f(x) { return x} // f é uma expressão de funçãop; x é a expressão de argumento.

let x = 1, y = 2, z = 3;
max = Math.max(x,y,z) // Math.max é a função; x, y e z são os argumentos.
console.log(max);

let a = [1,5,3,6,4,2];
a.sort() // a.sort é a função; não há argumentos.
console.log(a)
